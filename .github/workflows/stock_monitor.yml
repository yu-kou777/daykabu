name: Stock Sniper Auto Patrol

on:
  schedule:
    # 20分のタイムラグを考慮した設定（UTC表記なので日本時間-9時間）
    - cron: '30 0 * * 1-5'  # 日本時間 09:30 (前場・観測)
    - cron: '30 4 * * 1-5'  # 日本時間 13:30 (後場・観測)
    - cron: '10 6 * * 1-5'  # 日本時間 15:10 (大引け・報告)
  workflow_dispatch: # 手動でも動かせるボタンを出す設定

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Monitor Script
        run: python monitor_stocks.py  # 👈 ここを動かしたいファイル名に合わせてください
